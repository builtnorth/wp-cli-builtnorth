name: Tests

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  ci:
    uses: builtnorth/.github/.github/workflows/composer-package-ci.yml@main
    secrets: inherit
    with:
      composer-repository-profile: coding-standards-only
      # Package has no phpunit.xml, phpcs config, or composer phpcompat/lint scripts yet.
      run-coverage: false
      run-phpcs: false
      run-phpcompat: false
